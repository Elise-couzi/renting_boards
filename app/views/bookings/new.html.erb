<div class="container">
  <div style="margin: 50px"></div>

  <div class="row">
    <div class="col-lg-5">
      <p><%= image_tag @board.images, alt: "image", width: 400 %></p>
    </div>
    <div class="col-lg-7">
      <h1><%= @board.name %></h1>
      <h3><%= @board.price %>€</h3>
        <div class="form-wrapper" style="width: 600px; ">
          <h3>It's time to book man !</h3>
          <%= simple_form_for [ @board, @booking ] do |f| %>
          <%= f.input :start_at, as: :string, required: false, input_html: {class: "datepicker"} %>
          <%= f.input :end_at, as: :string, required: false, input_html: {class: "datepicker"} %>
          <%= f.button :submit, "On boarding", class: "btn btn-secondary" %>
          <% end %>
        </div>
    </div>
  </div>

  <div style="margin: 50px"></div>
</div>

